using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;
using Jint;

public class js_API : MonoBehaviour
{
	void Start() {
		/*
		var engine = new Engine().SetValue("log", new Action<object>(print));

		engine.Execute(@"
			function hello() {
				for(i=0;i<10;i++)
					log('Hello World: ' + i);
			};
			
			hello();
			");
		*/
	}

}
